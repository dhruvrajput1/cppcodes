#include <stdio.h>
int main() {
    
    int N1, N2, sum, avg;
    N1 = 10;
    N2 = 20;

    sum = N1 + N2;
    avg = (N1 + N2)/2;
    
    printf("Sum is %d \n",sum);
    printf("Average is %d \n", avg);

    return 0;
}
